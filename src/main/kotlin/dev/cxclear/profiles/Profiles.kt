package dev.cxclear.profiles

/**
 * 清理名单已按工具拆分到同目录：CodexProfile.kt / ClaudeCodeProfile.kt / CursorProfile.kt /
 * OpenCodeProfile.kt / DeepSeekHermesProfile.kt，入口是 AllProfiles.kt 的 `ALL_PROFILES`。
 * 改清理数据请去对应工具的 profile 文件；路径 helper 与永久保护路径在 ProfilePaths.kt。
 */
